import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'VIGILANTEye - Team Portfolio',
  description: 'Professional video surveillance management system with AI-powered face recognition and real-time alerting',
  keywords: 'video surveillance, face recognition, AI, computer vision, Flask, Python',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

